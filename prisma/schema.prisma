// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}


model User {

  id        Int      @id @default(autoincrement())
  email     String   @unique
  password  String    
  name      String
  role      String @db.VarChar(20)
  createdAt  DateTime   @default(now())

 }


 model Product{

  id                String      @id @default(uuid())
  barcode           String?      @db.VarChar(60) @unique 
  name              String
  description       String      @db.VarChar(400)
  price             Float 
  quantity_stock    Int         @default(0)
  createdAt         DateTime    @default(now())
  updatedAt         DateTime    @default(now()) @updatedAt
  category          Category    @relation(fields: [categoryId], references: [id])
  categoryId        Int
 }


model Category{

  id        Int @id @default(autoincrement())
  name      String @unique
  products  Product[]

 }




    // return [2 /*return*/, next(new appError_1.AppError(http_status_codes_1.StatusCodes.BAD_REQUEST, \"\", { category: { param: \"category\", msg: \"Need to provide an existant category\", location: \"body\", value: null } }))];\n   70 }\n   71 console.log(\"Before the attempt to create the product\");\n→  72 return [4 /*yield*/, server_1.prisma.product.create({\n        data: {\n          name: 'Mas color 1L',\n          price: 24.54,\n      +   barcode: String,\n          description: 'Default product description',\n          categoryId: 1,\n          ~~~~~~~~~~\n      ?   id?: String,\n      ?   quantity_stock?: Int,\n      ?   createdAt?: DateTime,\n      ?   updatedAt?: DateTime,\n      ?   category?: {\n      ?     create?: CategoryCreateWithoutProductsInput | CategoryUncheckedCreateWithoutProductsInput,\n      ?     connectOrCreate?: CategoryCreateOrConnectWithoutProductsInput,\n      ?     connect?: CategoryWhereUniqueInput\n      ?   }\n        }\n      })\n\nUnknown arg `categoryId` in data.categoryId for type ProductCreateInput. Did you mean `category`?\nArgument barcode for data.barcode is missing.\n\nNote: Lines with + are required, lines with ? are optional.\n